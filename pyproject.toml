[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "optionspricer"
version = "0.1"
dependencies = [
    "numpy",
    "scipy",
    "pandas",
    "matplotlib",
    "networkx",
]

[tool.hatch.build.targets.wheel]
packages = [
    "optionspricer"
]